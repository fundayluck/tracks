import React from "react";
import { Text, StyleSheet } from "react-native";

const Map = () => {
  return <Text>Map Location in here</Text>;
};

const styles = StyleSheet.create({});

export default Map;
